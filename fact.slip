main () { x = 20000 ; 
          y = fact(x) ;
          write (y) ;
      
        }

fact (x) { z = 1 ;

    while ( x != 0 ) { z = z * x ;
                       x = x - 1 ; 
                       }

           return (z) ; }

